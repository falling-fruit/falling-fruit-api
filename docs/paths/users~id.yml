get:
  summary: View a user (public).
  description: Forbidden for private users (`private` is true).
  tags:
    - Users
  security:
    - key: []
  parameters:
    - $ref: ../components/parameters.yml#/user_id
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            $ref: ../components/schemas.yml#/PublicUser
