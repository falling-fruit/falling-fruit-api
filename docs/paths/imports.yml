get:
  summary: Fetch Imports.
  tags:
    - Imports
  security:
    - key: []
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../components/schemas.yml#/Import
